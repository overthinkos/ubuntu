#  Per-kind file for pod entries. Sibling of charly.yml — imported via
#  charly.yml's `import:` list. The schema kind discriminator is
#  `kind: pod` for kind-keyed entries; root-shape map is `pod:` (this file).
#  2026-05 kind-files cutover.
version: 2026.159.1912
pod: {}
