#  Per-kind file for pod entries. Sibling of overthink.yml — included via
#  overthink.yml's `includes:` list. The schema kind discriminator is
#  `kind: pod` for kind-keyed entries; root-shape map is `pod:` (this file).
#  2026-05 kind-files cutover.
version: 2026.157.311
pod: {}
