#  Per-kind file for k8s entries. Sibling of overthink.yml — included via
#  overthink.yml's `includes:` list. The schema kind discriminator is
#  `kind: k8s` for kind-keyed entries; root-shape map is `k8s:` (this file).
#  2026-05 kind-files cutover.
version: 2026.156.557
k8s: {}
