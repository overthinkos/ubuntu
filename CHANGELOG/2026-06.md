# Changelog — 2026-06

### 2026-06-23 — chore(box/ubuntu): re-stamp to schema 2026.174.0900 + migrate package checks/acts to plugin: package

The `package` verb (the second typed-step state-provision verb: a check probe AND a run-act that lowers
to the TYPED `SystemPackagesStep` with reversals — reverse-remove + COPR-disable) left the closed `#Op`
into a dedicated builtin plugin unit (charly superproject — reusing the TypedStepProvider seam landed
with `service`). `charly migrate` re-stamps this repo's `charly.yml` + every discovered candy,
converting `package:` check AND run steps to `plugin: package` + `plugin_input` (package/installed/
version/package_map move into `#PackageInput`; `exclude_distro` stays a SHARED `#Op` skip-filter).
Gate: `charly box validate` exit 0 (git-workflow B7).

### 2026-06-23 — chore(box/ubuntu): re-stamp to schema 2026.174.0700 + migrate service checks/acts to plugin: service

The `service` verb (a typed-step state-provision verb: a check probe AND a run-act that lowers to the
TYPED `ServicePackagedStep` with load-bearing reversals) left the closed `#Op` into a dedicated builtin
plugin unit (charly superproject — via the new TypedStepProvider seam that preserves the typed step +
its reverse-ops for a `plugin: service` run step). `charly migrate` re-stamps this repo's `charly.yml`
+ every discovered candy, converting `service:` check AND run steps to `plugin: service` + `plugin_input`
(service/running/enabled move into `#ServiceInput`). Gate: `charly box validate` exit 0 (git-workflow B7).

### 2026-06-23 — chore(box/ubuntu): adopt @github candy pins to v2026.174.0425 (version-adoption)

`charly box reconcile --remote` rewrote 37 `@github.com/overthinkos/overthink/candy/*` pins from their
far-behind tags to `v2026.174.0425` (main's HEAD = the candy verb cutover). This is the B7-step-5
version-adoption that follows the `command` extraction: its restored `plugin_input` runtime-var check
correctly flagged a build-scope `${HOST_PORT}` bug in FROZEN pinned candy copies (fixed at the producer
HEAD); adopting the current pins clears the lag. The adopted candies are byte-identical to the
producer's R10'd local tree (same tag). Gate: `charly box validate` standalone exit 0.

### 2026-06-23 — chore(box/ubuntu): re-stamp to schema 2026.174.0500 + migrate command checks/acts to plugin: command

The dual-natured `command` verb (a check probe AND a run-act that lowers to the full `emitCmd` install
path) left the closed `#Op` verb set into a dedicated builtin plugin unit (charly superproject — the
hardest state-provision-tier verb). `charly migrate` re-stamps this repo's `charly.yml` + every
discovered candy and converts `command:` check AND run steps to `plugin: command` + `plugin_input`
(only the command-exclusive `command`/`background`/`from_host`/`in_container` move; the shared
matchers `stdout`/`stderr`/`exit_status` stay in `#Op`, and `command` itself stays an `#Op` modifier
read by the live `wl`/`libvirt` verbs). Gate: `charly box validate` exit 0 (git-workflow B7).

### 2026-06-23 — chore(box/ubuntu): re-stamp to schema 2026.174.0300 + migrate user/mount/kernel-param checks to plugin:

The state-provision verbs user, mount, kernel-param left the closed `#Op` into dedicated builtin
plugin units (charly superproject — the shared-path state-provision batch, on the proven act-emit
enabler). `charly migrate` re-stamps this repo's `charly.yml` to the new schema HEAD and converts any
`user:`/`mount:`/`kernel-param:` check/run steps to `plugin: <verb>` + `plugin_input` (each verb's
own fields move into its `#<Verb>Input`). Gate: `charly box validate` exit 0 (git-workflow B7).

### 2026-06-23 — chore(box/ubuntu): re-stamp to schema 2026.174.0100 + migrate unix_group checks to plugin:

The unix_group state-provision verb left the closed `#Op` into a dedicated builtin plugin unit
(charly superproject — the FIRST state-provision verb, dual-natured: a check-verb AND a run-verb→act).
`charly migrate` re-stamps this repo's `charly.yml` to the new schema HEAD and converts any
`unix_group:` check/run steps to `plugin: unix_group` + `plugin_input` (the shared `gid` companion,
read by the `user` verb, stays in `#Op`). Gate: `charly box validate` exit 0 (git-workflow B7).

### 2026-06-22 — chore(box/ubuntu): re-stamp to schema 2026.173.2300 + migrate http/interface/addr checks to plugin:

The observe verbs http/interface/addr left the closed `#Op` into dedicated builtin plugin units
(charly superproject, observe-verb wave 2). `charly migrate` re-stamps this repo's `charly.yml` to
the new schema HEAD and converts any `http:`/`interface:`/`addr:` CHECK steps to `plugin: <verb>` +
`plugin_input:` (the shared `method`/`request_body`/`timeout` modifiers stay in `#Op` — they're
read by the live cdp/dbus/libvirt verbs too). Gate: `charly box validate` exit 0 (git-workflow B7).

### 2026-06-22 — chore(box/ubuntu): re-stamp to schema 2026.173.1742 + migrate goss-verb checks to plugin:

The observe-only goss verbs (port/process/dns) left the closed `#Op` into dedicated builtin
plugin units (charly superproject). `charly migrate` re-stamps this repo's `charly.yml` to the
new schema HEAD and converts any `port:`/`process:`/`dns:` CHECK steps to `plugin: <verb>` +
`plugin_input:` (a box's published `port:` + install `package:`/`service:` are isStepNode-gated
and untouched). Gate: `charly box validate` exit 0 (git-workflow B7 schema-HEAD re-stamp).

### 2026-06-22 — chore(box/ubuntu): re-stamp to schema 2026.173.1616 (matching-to-plugin cutover)

The `matching` check verb left the closed `#Op` schema (the charly superproject added a dedicated
matching plugin unit + the `matching-to-plugin` MigrationStep raising `LatestSchemaVersion`
2026.172.0006 → 2026.173.1616). `charly migrate` re-stamps this repo's `charly.yml` `version:` to
the new schema HEAD; no content change here (matching was already migrated / unused). Gate:
`charly box validate` exit 0 (git-workflow B7 schema-HEAD re-stamp).

### 2026-06-21 — chore(box/ubuntu): migrate to schema 2026.172.0006 (EDGE-INHERIT cutover D — `box:` merged into `candy:`)

`charly migrate` applies the superproject's EDGE-INHERIT cutover D (the `box:` KIND merges INTO `candy:` —
`base:`/`from:` presence makes a candy a full IMAGE) to this submodule's per-box dirs: every discovered
`box/<name>/charly.yml` image renames its `box:` KIND discriminator to `candy:` (keeping its `base:`/`from:`
marker — including the `ubuntu-debootstrap` bootstrap builder's `from: builder:debootstrap`, which is what
makes it an image), and the schema HEAD is re-stamped to `2026.172.0006`. No behavior change — the image is
identical, only the kind-at-edge spelling moves. (R10 ran the check-ubuntu-coder-pod bed — PASS.)

### 2026-06-21 — chore(box/ubuntu): migrate to schema 2026.172.0004 (EDGE-INHERIT cutover B — `bundle:` eliminated)

`charly migrate` applies the superproject's EDGE-INHERIT cutover B (the `bundle:` deploy KIND is eliminated —
substrate kinds at the EDGE) to this submodule's `charly.yml`: every disposable check bed / deploy moves from
`bundle:{box:X}` to `pod:{image:X}` (and `bundle:{vm:V}`→`vm:{from:V}`, a targetless `bundle:`→`group:`),
and the schema HEAD is re-stamped to `2026.172.0004`. No behavior change — the deploy shape is identical, only
the kind-at-edge spelling moves. (R10 for the cutover ran its beds — PASS.)

### 2026-06-21 — chore(box/ubuntu): migrate to schema 2026.172.0002 (EDGE-INHERIT cutover A probe-verb rename)

`charly migrate` re-stamps the submodule to the new schema HEAD `2026.172.0002` for the superproject's
EDGE-INHERIT cutover A (the step probe verbs `k8s`/`group` → `kube`/`unix_group`). This submodule declares
no colliding step verb, so the only change is the version re-stamp; no behavior change.

### 2026-06-19 — docs: sweep stale build.yml/box.yml composition refs to the embedded charly.yml model (`v2026.170.1611`)

CLAUDE.md + README.md + the charly.yml comment headers described the retired `build.yml`
import + `box.yml` filename + per-kind sibling files + `charly`-namespace import. The
single-filename (2026.160.1300) + embedded-config (2026.165.1047) migrations dropped
`build.yml` (the distro/builder/init vocabulary is now embedded in the `charly` binary at
`charly/charly.yml`), renamed `box.yml` → `charly.yml`, and folded the per-kind siblings;
`import:` is `[]`. Comment-only / `*.md` edits — `charly box validate` still passes.

---

### 2026-06-19 — feat: adopt v2026.170.1105 candy pins (`v2026.170.1539`)

`charly box reconcile --remote` bumped all 37 `@github.com/overthinkos/*` pins (candy refs + the
`charly:` main-namespace import) from v2026.166.1222 → v2026.170.1105. Standalone `charly box validate`
(no `CHARLY_REPO_OVERRIDE`) passes against the real remote tag. Closes the §3 cutover's deferred candy-pin lag.

---

### 2026-06-19 — feat!: migrate to the unified name-first node model (`v2026.170.1105`)

`charly.yml` migrates to the unified `<name>: {<kind>: …, <children>}` node form
(deploy→bundle, venue/membership from tree position, `${HOST:…}` the sole
cross-member address). Validated via the local-main override; the standalone candy
`@github` pins bump in the immediate-next `charly box reconcile` cutover. Full
change: superproject `CHANGELOG/`.

---

### 2026-06-17 — chore(changelog): adopt the per-repo monthly `CHANGELOG/` structure

This repository now keeps its own `CHANGELOG/` directory with one file per
calendar month (`YYYY-MM.md`), matching the project-wide history model in which
the superproject and every submodule keeps its OWN `CHANGELOG/`. History is
repo-scoped, never centralized in a single root file. The repo's signpost
(`CLAUDE.md` / `README.md`) was updated to point here. See the superproject
`CLAUDE.md` "Where things are documented" and `/charly-internals:skills`.
